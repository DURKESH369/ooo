<!DOCTYPE html>
<html>
<head>
    <title>Hospital Patient Record System</title>

    <style>
        body{
            font-family: Arial, sans-serif;
            background:#f4f4f4;
            margin:0;
            padding:20px;
        }

        h1{
            background:#003366;
            color:white;
            text-align:center;
            padding:20px;
        }

        .container{
            display:flex;
            gap:20px;
        }

        .box{
            width:50%;
            background:white;
            padding:20px;
            border-radius:10px;
            box-shadow:0px 0px 10px gray;
        }

        .box h2{
            color:#003366;
        }

        input{
            width:95%;
            padding:10px;
            margin:8px 0;
        }

        button{
            padding:10px 20px;
            border:none;
            color:white;
            cursor:pointer;
        }

        .save{
            background:green;
        }

        .search{
            background:blue;
        }

        #output{
            margin-top:20px;
            background:#f9f9f9;
            padding:15px;
            border-radius:10px;
        }
    </style>
</head>

<body>

<h1>Hospital Patient Record System</h1>

<div class="container">

    <div class="box">
        <h2>Add Patient</h2>

        <input type="number" id="id" placeholder="Patient ID">

        <input type="text" id="name" placeholder="Patient Name">

        <input type="number" id="age" placeholder="Age">

        <input type="text" id="disease" placeholder="Disease">

        <br><br>

        <button class="save" onclick="savePatient()">
            Save Patient
        </button>
    </div>

    <div class="box">
        <h2>Search Patient</h2>

        <input type="number" id="searchId" placeholder="Enter Patient ID">

        <button class="search" onclick="searchPatient()">
            Search
        </button>

        <div id="output"></div>
    </div>

</div>

<script src="pat.js"></script>

</body>
</html>













let patients: any[] = [];

function savePatient() {

    let patient = {
        id: (document.getElementById("id") as HTMLInputElement).value,
        name: (document.getElementById("name") as HTMLInputElement).value,
        age: (document.getElementById("age") as HTMLInputElement).value,
        disease: (document.getElementById("disease") as HTMLInputElement).value
    };

    patients.push(patient);

    alert("Patient Saved Successfully");
}

function searchPatient() {

    let searchId =
        (document.getElementById("searchId") as HTMLInputElement).value;

    let patient = patients.find(p => p.id === searchId);

    if (patient) {

        document.getElementById("output")!.innerHTML =
        `
        <h3>Patient Details</h3>
        <p>ID: ${patient.id}</p>
        <p>Name: ${patient.name}</p>
        <p>Age: ${patient.age}</p>
        <p>Disease: ${patient.disease}</p>
        `;
    }
    else {

        document.getElementById("output")!.innerHTML =
        "<h3>Patient Not Found</h3>";
    }
}